# Codex Instructions

本仓库的正式入口是 `SKILL.md`。

| 任务 | 必读 | 执行方式 |
|---|---|---|
| 调整 Clash 配置 | `SKILL.md` + `config.yaml` | 保守修改，保持策略组语义稳定 |
| 调整自定义规则 | `SKILL.md` + 相关 `Rullset/*.list` + `config.yaml` | 同步 provider 和 rules |
| 审查维护性 | `SKILL.md` + `config.yaml` + `Rullset/` | 先补入口和约束，再做运行时重构 |

新任务开始前重读 `SKILL.md`。
